#cloud-config

packages: *kde-packages

runcmd: *desktop-runcmd

write_files:
  - *shared-directory          # Setup shared directory
  - *systemd-network           # Network configuration for server
  - *disable-printk-ratelimit  # Disable printk rate limiting
